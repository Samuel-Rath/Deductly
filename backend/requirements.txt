# Core FastAPI dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Data processing
pandas>=2.1.0
python-multipart>=0.0.6

# PDF parsing
PyPDF2>=3.0.0
pdfplumber>=0.10.0

# Fuzzy matching
rapidfuzz>=3.6.0

# PDF generation (ReportLab is more Windows-friendly than WeasyPrint)
reportlab>=4.0.0
# Alternative: weasyprint>=60.0 (requires GTK+ on Windows - see README for setup)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
hypothesis>=6.98.0
pytest-cov>=4.1.0
httpx>=0.27.0  # Required for FastAPI TestClient

# RAG / LLM — fitness tax scanner
anthropic>=0.34.0

# Utilities
python-dateutil>=2.8.0

# Logging and Monitoring (optional but recommended)
# structlog>=24.1.0  # Uncomment for structured logging
# sentry-sdk>=1.40.0  # Uncomment for error tracking
